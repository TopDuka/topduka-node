export interface StoreConfig {
  id: string;
  vat_enabled: boolean;
  vat_rate: number;
  prices_include_tax: boolean;
  currency_code: string;
  store_name: string;
  store_email: string;
  store_phone: string;
  store_address: string;
  store_logo: string;
  created_at: string;
}
